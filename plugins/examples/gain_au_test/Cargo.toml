[package]
name = "gain_au_test"
version = "0.1.0"
edition = "2021"
authors = ["NIH-Plug Contributors"]
description = "A simple gain plugin for testing AU functionality"
license = "ISC"
repository = "https://github.com/robbert-vdh/nih-plug"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
nih_plug = { path = "../../..", features = ["au"] }

[features]
default = ["au"]
au = []